#!/bin/bash
sed -i 's/Ok(())/self.conn.execute("CREATE TABLE IF NOT EXISTS vitals (id INTEGER PRIMARY KEY AUTOINCREMENT, patient_id INTEGER NOT NULL, heart_rate REAL, blood_pressure TEXT, temp REAL, timestamp DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY(patient_id) REFERENCES patients_global(id))", [])?;\n        self.conn.execute("CREATE TABLE IF NOT EXISTS allergies_and_conditions (id INTEGER PRIMARY KEY AUTOINCREMENT, patient_id INTEGER NOT NULL, condition TEXT NOT NULL, severity TEXT, FOREIGN KEY(patient_id) REFERENCES patients_global(id))", [])?;\n        Ok(())/' omni_med_nexus_os/core_engine/src/database.rs
