[package]
name = "rust_lib_ui_app"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
core_engine = { version = "0.1.0", path = "../../core_engine" }
flutter_rust_bridge = "=2.12.0"
lazy_static = "1.5.0"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(frb_expand)'] }
